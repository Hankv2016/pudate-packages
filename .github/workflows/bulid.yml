name: build

on:
  workflow_dispatch:
    inputs:
      base_version:
        description: '基准版本'
        required: true
        default: '3.10.11'
      target_version:
        description: '目标版本'
        required: true
        default: '3.10.21'
      major_minor:
        description: '主版本目录'
        required: true
        default: '3.10'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout pudate-packages
        uses: actions/checkout@v4
        with:
          path: repo

      - name: Checkout base CPython
        uses: actions/checkout@v4
        with:
          repository: python/cpython
          ref: v${{ inputs.base_version }}
          path: cpython_base
          fetch-depth: 1

      - name: Checkout target CPython
        uses: actions/checkout@v4
        with:
          repository: python/cpython
          ref: v${{ inputs.target_version }}
          path: cpython_target
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2

      - name: Prepare externals (base)
        shell: cmd
        run: |
          cd cpython_base\PCbuild
          build.bat -e -p x64
        continue-on-error: true

      - name: Build base CPython (force v143)
        shell: cmd
        run: |
          cd cpython_base\PCbuild
          msbuild pcbuild.proj /t:Build /m /nologo /v:m /p:Configuration=Release /p:Platform=x64 /p:PlatformToolset=v143 /p:IncludeExternals=false /p:IncludeCTypes=true /p:IncludeSSL=true /p:IncludeTkinter=true

      - name: Prepare externals (target)
        shell: cmd
        run: |
          cd cpython_target\PCbuild
          build.bat -e -p x64
        continue-on-error: true

      - name: Build target CPython (force v143)
        shell: cmd
        run: |
          cd cpython_target\PCbuild
          msbuild pcbuild.proj /t:Build /m /nologo /v:m /p:Configuration=Release /p:Platform=x64 /p:PlatformToolset=v143 /p:IncludeExternals=false /p:IncludeCTypes=true /p:IncludeSSL=true /p:IncludeTkinter=true
      
      - name: Install dependencies
        shell: pwsh
        run: python -m pip install msgpack zstd

      - name: Generate .pud
        shell: pwsh
        run: |
          cd repo
          python generate.py `
            --old "PCbuild/amd64;Lib" `
            --new "PCbuild/amd64;Lib" `
            --old-base ../cpython_base `
            --new-base ../cpython_target `
            --from ${{ inputs.base_version }} `
            --to ${{ inputs.target_version }} `
            --output ${{ inputs.major_minor }}/pudate_${{ inputs.target_version }}.pud

      - name: Update releases.json
        shell: pwsh
        run: |
          cd repo
          if (!(Test-Path releases.json)) { '{}' | Set-Content releases.json }
          $data = Get-Content releases.json | ConvertFrom-Json
          $entry = @{
            base = "${{ inputs.base_version }}"
            latest = "${{ inputs.target_version }}"
            pud = "${{ inputs.major_minor }}/pudate_${{ inputs.target_version }}.pud"
          }
          $data | Add-Member -NotePropertyName "${{ inputs.major_minor }}" -NotePropertyValue $entry -Force
          $data | ConvertTo-Json -Depth 10 | Set-Content releases.json

      - name: Commit and push
        shell: pwsh
        run: |
          cd repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add releases.json ${{ inputs.major_minor }}/
          git diff --staged --quiet || git commit -m "Add pudate_${{ inputs.target_version }}.pud"
          git push